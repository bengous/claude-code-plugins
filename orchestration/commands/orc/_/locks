## Lock Management

**Acquire lock:**
```bash
mkdir -p .claude/run/locks && touch .claude/run/locks/<target-branch>.lock
```
If lock exists, abort with clear message.

**Release lock:**
```bash
rm -f .claude/run/locks/<target-branch>.lock
```
