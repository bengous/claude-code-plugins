{
  "description": "Worktree guard hook that blocks raw git worktree commands to enforce managed worktree system",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "/home/b3ngous/projects/claude-plugins/orchestration/hooks/worktree-guard.py",
            "timeout": 5,
            "description": "Blocks raw git worktree commands to enforce managed worktree system"
          }
        ]
      }
    ]
  }
}
